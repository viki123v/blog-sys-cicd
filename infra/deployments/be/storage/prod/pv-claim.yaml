apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: be-pv-claim
  namespace: blog-prod
spec:
  storageClassName: efs-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi